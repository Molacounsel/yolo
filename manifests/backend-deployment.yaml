apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: mola-yolo-backend
        image: counselmola/yolo-backend:v1.0.0
        ports:
        - containerPort: 5000
